# Image Panel

The v0.1 image panel renderer lives in `apps/local-studio` and the protocol/runtime packages. This directory is reserved for extraction into a first-party package after the runtime API stabilizes.
