{
  "name": "@openpanels/local-control",
  "version": "0.1.6",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest --run"
  },
  "dependencies": {
    "@openpanels/local-storage": "workspace:*",
    "@openpanels/protocol": "workspace:*",
    "@openpanels/runtime": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^25.3.5",
    "typescript": "^5.9.3",
    "vitest": "^4.0.18"
  }
}
